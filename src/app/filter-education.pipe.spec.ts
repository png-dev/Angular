import { FilterEducationPipe } from './filter-education.pipe';

describe('FilterEducationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEducationPipe();
    expect(pipe).toBeTruthy();
  });
});
